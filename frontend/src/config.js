// src/config.js
const config = {
    // API base URL configuration
    apiUrl: process.env.REACT_APP_API_URL || 'http://localhost:8000/api',
    // Application title
    appTitle: 'Analytics Hub',
};

export default config;
